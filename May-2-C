#include<stdio.h>

int main()
{
    int m, n, i, j, k, count=0, sum=0;
    scanf("%d", &m);
    
    int arr[m][2];
    
    for(i=0 ; i<m ; i++)
    {
        for(j=0 ; j<2 ; j++)
        {
            scanf("%d", &arr[i][j]);
        }
    }
    
    scanf("%d", &n);
    int a[n][2];
    
    for(i=0 ; i<n ; i++)
    {
        for(j=0 ; j<2 ; j++)
        {
            scanf("%d", &a[i][j]);
        }
    }
    for(i=1 ; i<n ; i++)
    {
        a[i][1] = a[i][1] + a[i-1][1];
    }

    for(i=0 ; i<m ; i++)
    {
        for(j=0 ; j<n ; j++)
        {
            if(a[j][0]<=arr[i][0])
            {
                if(a[j][1]>=arr[i][1])
                {
                    count++;
                }
            }
        }
    }
    
    if(m%2==0)
    {
        if(count>=m/2)
        {
            printf("Allowed");
        }
        else
        {
            printf("Denied");
        }
    }
    else if(m%2==1)
    {
        if(count>=(m+1)/2)
        {
            printf("Allowed");
        }
        else
        {
            printf("Denied");
        }
    }
    return 0;
}
